# 接口自动化测试框架依赖（模块 14 CI/CD 使用）
# 版本根据本地 android_project conda 环境（Python 3.12.13）实际安装锁定

# 测试框架
pytest==9.0.3
pytest-rerunfailures==16.3
pytest-xdist==3.8.0

# 报告
allure-pytest==2.16.0

# HTTP 客户端
requests==2.34.2

# YAML 解析
PyYAML==6.0.2

# 环境变量管理（加载 .env 文件中的敏感配置）
# 注意：当前 android_project 环境未安装，需手动安装
python-dotenv==1.2.2

# JSON 路径提取
jsonpath==0.82.2

# Mock Server
Flask==3.1.3

# 日志
loguru==0.7.3

# 数据库
PyMySQL==1.2.0
redis==7.1.0

# JSON Schema 校验（可选，框架扩展用）
jsonschema==4.26.0

# Excel 读取（可选）
xlrd==2.0.2
openpyxl==3.1.5
